[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]
cacheDirectories = ["/root/.npm", "/app/node_modules", "/root/.cache/prisma"]

[phases.build]
cmds = ["npm run db:generate", "npm run build"]

[start]
cmd = "npm start"
